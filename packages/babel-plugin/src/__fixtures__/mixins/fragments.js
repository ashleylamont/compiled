// eslint-disable-next-line import/no-extraneous-dependencies
import { cssFragment } from '@compiled/vanilla';

export const dangerHighlight = cssFragment({
  borderColor: 'red',
  borderWidth: 2,
  borderStyle: 'solid',
});

export const baseStyles = {
  color: 'red',
  fontSize: 14,
};
