---
'@compiled/react': minor
---

Runtime utilities (`ax`, `ac`, `insertRule`, `isServerEnvironment`, etc.) are now also available from the new `@compiled/runtime` package. `@compiled/react` continues to re-export all of them for full backward compatibility.
