---
'@compiled/babel-plugin': minor
---

`@compiled/vanilla` is now a recognised import source. The Babel plugin handles `cssMap`, `classNames`, `globalStylesheet`, and `cssFragment` calls from `@compiled/vanilla`. New `classNames()` → `ax()` transform and `globalStylesheet()` non-atomic CSS extraction.
