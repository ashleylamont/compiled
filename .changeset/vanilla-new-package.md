---
'@compiled/vanilla': minor
---

New `@compiled/vanilla` package: compile-time CSS-in-JS without a React dependency. Provides `cssMap`, `classNames`, `globalStylesheet`, and `cssFragment` APIs for use in non-React environments such as ProseMirror `toDOM` functions.
